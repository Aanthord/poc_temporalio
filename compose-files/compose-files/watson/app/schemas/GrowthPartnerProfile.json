{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "id": "GrowthPartnerProfile",
    "allOf": [{ "$ref": "file:TalentpairMetadata.json" }],
    "title": "Talentpair Token Metadata extension",
    "type": "object",
    "properties": {
        "growth_partner_uiid": {
            "description": "The unique identifier for a GrowthParter user",
            "type": "string",
            "format": "uuid"
        },
        "agency_id": {
            "description": "The unique id to the growth partners org",
            "type": "integer"
        },
        "fow_specialties": {
            "type": "array",
            "description": "Array of field of work.",
            "items": {
                "type": "object",
                "description": "Defines a set of attributes which classifies and defines the FOW. Allows for applications to build fitlers and attribute search functionalities for a set of FOWs."
            }
        }

    }
}